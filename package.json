{
  "name": "melodia",
  "version": "1.0.0",
  "description": "MELODIA - Voz, Vídeo, Tela 60FPS, Chat e Amigos",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "app": "concurrently -n \"SERVER,DESKTOP\" -c \"bgBlue.bold,bgGreen.bold\" \"npm run dev --prefix server\" \"npm run electron:dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "build:exe": "npm run electron:build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
